# API URL for backend server
# VITE_API_URL=http://localhost:5001/api
# Để trống để frontend tự nhận host hiện tại (hỗ trợ localhost + LAN).
# Ví dụ chạy LAN: VITE_API_URL=http://192.168.1.100:5001/api